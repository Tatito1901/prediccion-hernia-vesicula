# Supabase project configuration
# Copy this file to .env.local and fill with your actual credentials
# NOTE: Do NOT commit .env.local

# Public (safe for client-side). Required by utils/supabase/server.ts and client.ts
NEXT_PUBLIC_SUPABASE_URL="https://YOUR-PROJECT.ref.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="YOUR_ANON_PUBLIC_KEY"

# Server-only (never expose in client). Required by utils/supabase/admin.ts for admin routes
SUPABASE_SERVICE_ROLE_KEY="YOUR_SERVICE_ROLE_KEY"
